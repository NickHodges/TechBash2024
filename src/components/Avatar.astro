---
const fetchAvatar = async () => {
  await new Promise((res) => setTimeout(res, 1500));
  return '/public/NickAvatar.jpg';
};

const avatarImage = await fetchAvatar();
---

<img src={avatarImage} alt="Your avatar" />
