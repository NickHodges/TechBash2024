---
function generateRandomString(length: number = 10): string {
  const characters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
  const charactersLength = characters.length;
  let result = '';

  for (let i = 0; i < length; i++) {
    const randomIndex = Math.floor(Math.random() * charactersLength);
    result += characters[randomIndex];
  }

  return result;
}

const fetchAvatar = async (): Promise<string> => {
  await new Promise((res) => setTimeout(res, 1500));

  const randomString = generateRandomString(); // Generates a random 10-character string
  const avatar = `https://robohash.org/${randomString}?size=88x88`; // Constructs the URL using the random string
  return avatar;
};

const avatarImage = await fetchAvatar();
---

<img src={avatarImage} alt="Your avatar" />
