---
import type { Quote } from '../utils/fetchQuote';

interface QuoteProps {
  theQuote: Quote;
}

const { theQuote } = Astro.props as QuoteProps;
---

<div class="quote-container">
  <blockquote>
    <p><a href=`/quote/${theQuote.id}`>🔗</a> "{theQuote.quote}"</p>
    <footer>— {theQuote.author}</footer>
  </blockquote>
</div>

<style>
  .quote-container {
    padding: 2rem;
    border: 1px solid #ccc;
    border-radius: 8px;
    background-color: #f9f9f9;
    max-width: 600px;
    margin: 2rem auto;
  }

  blockquote {
    font-style: italic;
    color: #555;
  }

  footer {
    text-align: right;
    font-size: 1rem;
    margin-top: 1rem;
  }
</style>
